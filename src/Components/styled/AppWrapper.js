import styled from 'styled-components';

const AppWrapper = styled.main`
  height: 100vh;
  width: 100vw;

  display: flex;
  justify-content: center;
  align-items: center;
`;

export { AppWrapper };
